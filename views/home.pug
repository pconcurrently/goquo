extends layout

block content
  section.tile-container
    img.sticky-image(src="images/skyline.png")
    section.tile-content-wrapper
    .tile-content
      .form-banner
        .welcome-message.loading
          h1 HOTELS, RESORTS, HOSTELS & MORE
        .search-box 
          .destination  
            i.search-icon.fa.fa-search
            .input-holder
              input#destination-input(placeholder="Enter destination or property", value="Ha Noi")
            .destination-search-results.popup-result
              ul
                li 
                  .result-item
                    i.fa.fa-hotel 
                    span &nbsp Ha Noi, Viet Nam
                li 
                  .result-item
                    i.fa.fa-star
                    span &nbsp Ha Noi Club, Viet Nam
                li 
                  .result-item
                    i.fa.fa-coffee
                    span &nbsp Ha Noi Central, Viet Nam
          .select-date
            .start-date
              .input-group
                span.input-group-addon
                  i.fa.fa-calendar
                input.form-control#start-date-picker
            .end-date
              .input-group
                span.input-group-addon
                  i.fa.fa-calendar
                input.form-control#end-date-picker
          .box-and-button      
            .occupancy-box
              i.box-icon.fa.fa-users
              .box-results
                .box-people
                  span.loading  3 adults, 0 children
                .box-rooms
                  span.loading  3 rooms
            .search-button  
              a.loading(href="/search") 
                span.loading.search-text Search
        .search-result-backdrop
    section.tile-promotion
        .col-md-6.square-tile.loading
          .detail
            h3 Drop prices up to 30%
            small Sign in now for member-only deal
            button.btn Sign in now
          img(src="images/promote1_background.png")
        .col-md-6.square-tile.loading
          .detail
            h3 Drop prices up to 30%
            small Sign in now for member-only deal
            button.btn Sign in now
          img(src="images/promote1_background.png")
        .col-md-12.double-square-tile.loading
          img(src="images/promote2_background.jpg", style="width: 100%")
        .col-md-6.square-tile.loading
          .detail
            h3 Drop prices up to 30%
            small Sign in now for member-only deal
            button.btn Sign in now
          img(src="images/promote1_background.png")
        .col-md-6.square-tile.loading
          .detail
            h3 Drop prices up to 30%
            small Sign in now for member-only deal
            button.btn Sign in now
          img(src="images/promote1_background.png")
        .col-md-12.double-square-tile.loading
          img(src="images/promote2_background.jpg", style="width: 100%")
        .col-md-6.square-tile.loading
          .detail
            h3 Drop prices up to 30%
            small Sign in now for member-only deal
            button.btn Sign in now
          img(src="images/promote1_background.png")
        .col-md-6.square-tile.loading
          .detail
            h3 Drop prices up to 30%
            small Sign in now for member-only deal
            button.btn Sign in now
          img(src="images/promote1_background.png")
block script
  script.
      setTimeout(function() {
            $(".loading").removeClass("loading");

            $('#start-date-picker').datetimepicker({
                format: 'DD MMM YYYY',
                defaultDate: moment()
            });
            $('#end-date-picker').datetimepicker({
                format: 'DD MMM YYYY',
                defaultDate: moment().add(1, 'days')
            });
            setAmounts();
        }, 3000)

      $('#destination-input').focus(function() {
        $('.destination-search-results').css('display', 'block');
        $('.search-result-backdrop').css('display', 'block');
      });

      $('.search-result-backdrop').click(function() {
        $(this).css('display', 'none');

        $('.popup-result').css('display', 'none');
      })
      $('.popup-result ul li').click(function() {
        $('.search-result-backdrop').css('display', 'none');
        $('.popup-result').css('display', 'none');
      })